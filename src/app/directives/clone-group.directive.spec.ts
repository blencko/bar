import { CloneGroupDirective } from './clone-group.directive';

describe('CloneGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new CloneGroupDirective();
    expect(directive).toBeTruthy();
  });
});
