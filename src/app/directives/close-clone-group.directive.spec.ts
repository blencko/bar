import { CloseCloneGroupDirective } from './close-clone-group.directive';

describe('CloseCloneGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseCloneGroupDirective();
    expect(directive).toBeTruthy();
  });
});
