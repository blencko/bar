import { DropifyDirective } from './dropify.directive';

describe('DropifyDirective', () => {
  it('should create an instance', () => {
    const directive = new DropifyDirective();
    expect(directive).toBeTruthy();
  });
});
