import { CeprequestDirective } from './ceprequest.directive';

describe('CeprequestDirective', () => {
  it('should create an instance', () => {
    const directive = new CeprequestDirective();
    expect(directive).toBeTruthy();
  });
});
