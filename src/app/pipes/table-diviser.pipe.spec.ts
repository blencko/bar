import { TableDiviserPipe } from './table-diviser.pipe';

describe('TableDiviserPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDiviserPipe();
    expect(pipe).toBeTruthy();
  });
});
